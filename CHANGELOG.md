# Change Log

## [0.0.1]

- scaffolded the extension with the official VS Code TypeScript generator
- added `.xlsx` compare commands
- added `fastxlsx` workbook loading and snapshot normalization
- added side-by-side paginated Webview diff UI
- added URI handler support for external compare launches
- added `scripts/xlsx-difftool.mjs` for Git difftool integration
